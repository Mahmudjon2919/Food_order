from django.urls import path, include

urlpatterns = [
    #path('users/', include('apps.users.urls')),
    #path('orders/', include('apps.orders.urls')),
     path('restaurants/', include('apps.restaurants.ursl'))
]
